{
  "name": "idastuudio-static-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Static mirror demo of idastuudio.ee with custom product catalog page",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --skipClone && npx serve public -l 5173",
    "clone": "bash scripts/clone.sh",
    "dev:npm": "npm run build -- --skipClone && npx serve public -l 5173"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "serve": "^13.0.4"
  },
  "packageManager": "pnpm@10.7.0"
}
